import logging


class Rom:
    romvar = ""

    def __init__(self):
        romvar = "init rom..."
        logging.debug(romvar)
        logging.debug("no hace nada aun")

    # def load(self, file):        logging.debug("LLego aqui")
